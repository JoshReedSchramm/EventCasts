<% if !@user.nil? %>
	<ul id="user_groups">
		<% for group in @user.groups %>
			<% if group.parent.nil? %>
			    <li><a href='/<%= group.full_path %>'>#<%= group.name %></a>
					<% if !group.groups.nil? %>
						<ul id="user_groups_sub_groups">
							<% group.groups.each do |sg| %>
								<li><a href='/<%= sg.full_path %>'>#<%= sg.name %></a></li>
							<% end %>
						</ul>
					<% end %>			
				</li>		
			<% end %>
		<% end %>
	</ul>	
<% end %>
