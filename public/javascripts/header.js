$(document).ready(function() {
	var pop_up_active = false;
	$('a#my_groups_header_link').cluetip({
		sticky: true, 
		closePosition: 'title',
		arrows: true,
		dropShadow: false,
		positionBy: 'bottomTop'
	});
});