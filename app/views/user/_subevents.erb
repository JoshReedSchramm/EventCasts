<% if !@user.nil? && !@user.events.nil? && !@user.events.empty?  %>
	<ul id="user_events">
		<% for event in @user.events %>
		    	<li><a href='/<%= event.id %>'>#<%= event.name %></a>
		  	<% end %>
		<% end %>
	</ul>	
<% end %>
