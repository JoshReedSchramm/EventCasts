<h3 id="search_results_header">Search Results:</h3>
<% if !@results.nil? %>
    <ul id='results_list'>
        <% for result in @results
            if !result.event.nil? %>    
                <li id='result_<% result.event.id %>' class='result'>
                    <span class='hashtag'><a href='/<%= result.event.name %>'>#<%= result.event.name %></a></span>
                    <span class='registration'>owner: <% if !result.event.owner.nil? %><a href='http://twitter.com/<%= result.event.owner.twitter_name %>' target='_blank'>@<%= result.event.owner.twitter_name %></a><% end %>
                      registered: <%= result.event.created_at.strftime("%m/%d/%Y") %>
                      </span>
                    <div class='clear'></div>
                     <hr class='tweet_rule' />
                  <div class='clear'></div>
                  <% if !result.event.title.nil? %>
                      <h4><%=result.event.title%></h4>
                      <span class="event_title"></span>
                      <div class='clear'></div>
                  <% end %>
                    <div class='description'><%= result.event.description %></div>
                    <div class='clear'></div>
                </li>
            <% end
        end %>
    </ul>
<% else %>
    No results found!
<% end %>
